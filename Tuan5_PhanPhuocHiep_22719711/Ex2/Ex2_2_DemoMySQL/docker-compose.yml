version: '3.8'
services:
  db:
    image: mysql:8.0
    environment:             
      MYSQL_USER: phuochiep
      MYSQL_PASSWORD: phuochiep123
      MYSQL_DATABASE: testdb
      MYSQL_ROOT_PASSWORD: testdb123
    ports:
      - "3306:3306"        
    volumes:
      - dbdata:/var/lib/mysql 

volumes:
  dbdata:
